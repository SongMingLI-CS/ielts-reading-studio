(()=>{const form=document.getElementById('practice-form');if(!form)return;const started=Date.now();const timer=document.getElementById('timer');setInterval(()=>{const seconds=Math.floor((Date.now()-started)/1000);timer.textContent=`${String(Math.floor(seconds/60)).padStart(2,'0')}:${String(seconds%60).padStart(2,'0')}`},1000);form.addEventListener('submit',async event=>{event.preventDefault();const answers={};for(const [key,value] of new FormData(form).entries())answers[key.replace(/^q/,'')]=value;const response=await fetch(`/practice/${form.dataset.unitId}/submit`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({answers,elapsed_seconds:Math.floor((Date.now()-started)/1000)})});const data=await response.json();const panel=document.getElementById('practice-result');panel.hidden=false;panel.innerHTML=`<h2>${data.correct} / ${data.total}</h2><p><a href="/practice/${form.dataset.unitId}/analysis">查看解析</a></p>`})})();
