# 项目状态（2026-09-16，第二批）

## 本轮完成

1. **全局词库补足到 5,942 条**（`data/vocabulary.json`，规范区间 5,000–7,000）
   - CEFR：B1 20.1% / B2 59.8% / C1 20.1%（规范 20/60/20）
   - 类型：动词 22.4% / 形容词副词 24.5% / 名词 23.8% / 固定搭配 20.6% / 短语动词 8.7%
   - 音标 **100%**（`work/fill_phonetics.py` 补齐 281 条，仅 1.4k 输入 + 6.2k 输出 tokens）、搭配 99.97%、例句 89.5%
   - 生成按 8 个叙事语义域轮转避免与书本重复；构建器改为**多轮自我校正**直到达标（`build(rounds=N)`）
2. **提示词调优**：规则 24 要求每段尽量含 1 个 C1 词；每块候选词窗口的 C1 占比 35% → 50%
3. **长跑稳定性修复（都是真实失败定位出来的）**
   - 传输层错误（SDK 的连接/超时异常不是 `TimeoutError` 子类）之前**不重试**，导致章节直接失败 → 现在按类名识别并指数退避重试
   - 模型把拼音漏进正文（如「抚mo」）会造出无释义的裸英文词 → 修复器直接删除这类碎片
   - 插词过多（37–49/500）会整章失败 → 新增**章级确定性密度修剪**（把最密段落的多余词条还原为中文）
   - `\b` 词边界在中文旁失效（「的give up（放弃）」被误判为词条不在正文）→ 改用 ASCII 前瞻/后顾断言
   - 长段落插入不足 → 分块门禁只挡「整体失败」（≥7/500），缺口交给章级补足：先模型补足（3 轮、逐轮缩小段落组），再**确定性补词**（用词库中文释义就地替换为 `english（中文释义）`，不破坏每句 ≤3 项）
   - `max_tokens` 过小导致 JSON 截断风暴（生成/标注）→ 生成 ≥6000、标注 60 条/批并支持二分重试
4. **批量实测**：第 1–30 章全部完成，**0 失败**；`--retry-failed` 可反复收敛失败章节

## 质量数据（第 27 章，含全部修复后的产物）

| 项目 | 结果 |
| --- | --- |
| 段落完整性 | 21/21，顺序一致，无缺段/多余段 |
| 还原中文比例 / 相似度 | 1.003 / 0.970 |
| 人名、数字变化 | 0 |
| 插词密度 | 79 项 / 1,967 字 = 20.08 / 500（段级 10.6–30.6） |
| 本章词表 | 40 行 × 8 列，音标覆盖 92% |
| DOCX 排版 | 79 个加粗英文 run + 158 个粉色底纹 run、章末分页 |

全书累计（29 章、2,844 个插入项）：CEFR 占比 B1 32.9% / B2 53.0% / C1 14.1%。

## 当前进度与命令

```powershell
python main.py --resume         # 继续处理（每次最多 20 章）
python main.py --retry-failed   # 只重跑失败章节
python main.py --chapter 31     # 单章
python work/build_full_catalogue.py <书1.epub> <书2.epub> --target=7000   # 词库补到 7000
python work/fill_phonetics.py --glossary                                   # 补术语表音标
```

- 已完成：第 1–30 章（`output/chapters/`、`output/html/`、`output/chapter_json/`）
- 每 20 章生成 `output/reports/check_reports/`，每 50 章生成 `output/volumes/第NN卷_….docx`

## 仍待处理

1. 正文 C1 占比 14.1%（词库 20.1%）：可继续提高候选窗口 C1 配比或专挑抽象语境；需在自然度与比例间取平衡。
2. 术语表（`output/glossary.json`）中模型自造词仍可能缺音标，可跑 `work/fill_phonetics.py --glossary` 补齐。
3. 词库可续补至 7,000 条（断点续跑）。
4. DOCX 视觉校验需要本机 LibreOffice；当前以结构化校验（加粗/底纹/分页/表格）替代。
